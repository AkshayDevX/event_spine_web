import { WorkflowForm } from "@/components/workflows/workflow-form";

export const metadata = {
  title: "Add Workflow | EventSpine",
};

export default function AddWorkflowPage() {
  return <WorkflowForm />;
}
